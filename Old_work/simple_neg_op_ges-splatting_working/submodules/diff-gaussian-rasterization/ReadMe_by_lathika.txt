To install with a default folder:   pip install .

To install with alternative rasterization method:   RASTERIZER_FLAG=$flag name$ pip install .

$flag name$ :-
non - default
neg_op  - gaussians with both positive or negative opacity